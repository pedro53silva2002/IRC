who command
std::transform(channelName.begin(), channelName.end(), channelName.begin(), ::tolower);
join can accept &?

/*

	if (!restOfCommand.empty())
		msg += " :" + restOfCommand + "\r\n";
	else
		msg += "\r\n";
*/